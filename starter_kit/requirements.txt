# LoomQ's main adapter uses only the Python standard library.
# Vendor SDKs are exactly pinned in requirements-{spinq,originq,braket}.txt
# and installed into isolated import layers by Dockerfile.
# pyqpanda==<tested-version>
